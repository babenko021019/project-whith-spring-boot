

    1. Instaling databases: PostgreSQL, H2 and MongoDB.

    2. Creating database "spring" in PostgreSQL.

    3. Creating testing database "spring_boot" in H2.

    4. After started main Class open the Base URL:
    http://localhost:8080/swagger-ui.html#/
    for to operation this API.
